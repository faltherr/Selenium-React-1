Load our application using Selenium by providing the required URI, get our app title once our application loads and checking it with the given appTitle value.

Find an element in our application to check whether the given element is loaded, which we are going to add it later in this post.